// Public entry for `@better-activity/react`.
// All real code lives under ./src — this file is a 1-line facade so the
// monorepo's `@packages/react/index.ts` reference resolves to the package
// surface.

export * from "./src/index";
